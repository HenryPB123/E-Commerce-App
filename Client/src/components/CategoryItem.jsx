import styled from "styled-components";

const Container = styled.div``;

// eslint-disable-next-line react/prop-types
const CategoryItem = () => {
  return <Container>categoria itme</Container>;
};

export default CategoryItem;
